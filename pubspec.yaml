name: alpr_watchlist
description: Scan plaques d’immatriculation (OCR) en flux vidéo + alerte watchlist (Android/iOS)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: 0.10.5+9
  google_mlkit_text_recognition: ^0.15.0
  shared_preferences: ^2.3.3

dependency_overrides:
  camera_android_camerax: 0.6.5+2
  camera_platform_interface: 2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
